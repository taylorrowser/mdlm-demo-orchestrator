Command:
node --test --test-name-pattern='nested-directory swap|reauthenticates index|tracked-byte limits' test/preflight.test.mjs

Result: FAIL (expected RED)

✖ raw cleanliness rejects a nested-directory swap and restore during public provenance inspection
  Expected values to be strictly equal: false !== true
  The post-hashing restore hook was not reached, so the existing implementation could not authenticate the swap-and-restore boundary.

✖ raw cleanliness reauthenticates index and untracked observations after hashing
  Expected values to be strictly equal: false !== true
  The existing implementation never performed the post-hashing observation hook.

✖ raw cleanliness enforces per-file and aggregate tracked-byte limits
  Expected values to be strictly equal: true !== false
  The existing implementation ignored the requested tracked-byte bound and authenticated the repository.

Tests: 3; pass: 0; fail: 3.
